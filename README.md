# IotaOS
A simple OS made for fun 

# Set Up GCC Cross Compiler

1. Install Build Dependencies
```bash
$ sudo apt update
$ sudo apt install bison flex libgmp3-dev libmpc-dev libmpfr-dev libisl-dev
$ guix texinfo@4.13a
```
> [!NOTE]
> **`guix`** may **not** be **installed** by defualt